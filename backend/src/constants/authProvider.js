/**
 * @fileoverview User authentication-provider constants.
 *
 * @module constants/authProvider
 */

// Identifies how a user account was created / authenticates.
export const AUTH_PROVIDER = Object.freeze({
  LOCAL: 'local',
  GOOGLE: 'google',
});
